# Anthropic — used by /api/analyse-run for vision + reactions
ANTHROPIC_API_KEY=

# Vercel KV (Upstash Redis under the hood) — populated by `vercel env pull`
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Vercel Blob — populated by `vercel env pull`
BLOB_READ_WRITE_TOKEN=
